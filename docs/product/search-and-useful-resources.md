# Search discovery and useful resources

Requested September 21, 2026: execute the Google Search Console setup and first-month distribution plan. Connect the existing domain property, preserve DNS and email settings, submit the sitemap, inspect priority URLs, link existing profiles, and provide useful companion material. Search submission is not evidence of indexing or ranking.

The companion tool is a free, browser-only cost-per-successful-task calculator for engineering leaders and builders. Enter one batch's attempted/successful tasks and total model, tool, infrastructure, and human-review costs; inspect total spend, completion rate, cost per attempted task, and cost per accepted outcome. Optional comparison uses the same batch and acceptance rule. The initial values reproduce the clearly labeled invented example in the existing cost essay. No provider prices, ROI claims, accounts, storage, network calls, or telemetry.

Inputs remain on the page and reset on refresh; copying exports an explicit plain-text summary, not a URL containing potentially sensitive cost information. Invalid/blank inputs cannot produce a plausible result. Zero successes must say no successful completions, not zero cost. Costs include all calls and retries across successful and failed tasks; human cost is minutes times hourly cost divided by 60. Use one currency and evaluation window for both designs. No success or quality forecast is computed.

The tool lives at `/tools/task-cost/`, linked from Projects and the cost essay. Static formula and worked example remain readable without JavaScript. Keep main navigation, identity, independent Agent Explainer, and existing editorial schedules intact. Test calculation/validation boundaries, edits, comparison, reset, denied clipboard/storage, keyboard, themes, narrow layouts, no-JavaScript explanation, local links, sitemap, and the JavaScript budget. Deploy through the existing passing Pages workflow; rollback baseline is `edc6f857718c434ddae458883b94265946d2a76a`.

Distribution: reuse the existing real Agent Explainer demonstration with a concise explanation and clear simulation label. Prepare a captioned video and LinkedIn copy. Week-four review should inspect Search Console impressions, clicks, queries, and pages after data has had time to accumulate; do not invent initial traffic or infer results from empty reports. No unsolicited messages to communities or people.
